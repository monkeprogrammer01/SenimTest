# Senim Technologies тестовое задание (FastAPI)

## 🚀 1. Как начать (Быстрый запуск)

### 1.1. Настройка окружения

1. **Клонируйте проект:**
   ```bash
   git clone https://github.com/monkeprogrammer01/SenimTest.git](https://github.com/monkeprogrammer01/SenimTest.git
   cd SenimTest
   ```

2. **Создайте и активируйте виртуальное окружение (ОБЯЗАТЕЛЬНО):**

   Это изолирует зависимости проекта от вашей основной системы.

   ```bash
   # Создать venv
   python -m venv venv
   
   # Активировать venv (для Linux/macOS)
   source venv/bin/activate
   
   # Активировать venv (для Windows Command Prompt)
   # .\venv\Scripts\activate
   ```

3. **Установите зависимости:**
   ```bash
   pip install -r requirements.txt
   ```

4. **Создайте `.env` файл:**
   В корне проекта создайте файл `.env` и укажите ключи
   ```
   JWT_SECRET_KEY=mysecret
   ENVIRONMENT=sqlite
   ```

### 1.2. База данных и запуск

1. **Примените миграции:**

   ```bash
   alembic revision --autogenerate -m "Create tables"
   ```

   Затем примените изменения к базе данных:
   ```bash
   alembic upgrade head
   ```

2. **Запустите сервер:**
   ```bash
   fastapi dev main.py
   ```

   Сервер доступен по адресу: **http://127.0.0.1:8000**
   **Документация API (Swagger UI):** **http://127.0.0.1:8000/docs**

## 🚀 2. Коротко о работе JWT

Клиент отправляет email (используется вместо стандартного username) и password на **auth/login** получает access_token.
Payload токена хранит в себе id пользователя.
**auth/me** работает с помощью метода getCurrentUser который получает токен и делает jwt.decode().
А эндпойнты **reports/** получают Authorization: Bearer token и определяет роль
если роль admin - видит все репорты, в случае если это staff видит только свои.
* Если роль **`admin`** — видит **все** отчеты.
* Если роль **`staff`** — видит **только свои** отчеты.