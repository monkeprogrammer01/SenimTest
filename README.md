# Senim Technologies тестовое задание (FastAPI)

## 🚀 1. Как начать (Быстрый запуск)

### 1.1. Настройка окружения

1.  **Клонируйте проект:**
    ```bash
    git clone https://github.com/monkeprogrammer01/SenimTest.git
    cd SenimTest
    ```

2.  **Установите зависимости:**
    ```bash
    pip install -r requirements.txt
    ```

3.  **Создайте `.env` файл:**
    В корне проекта создайте файл `.env` и укажите ключи:
    ```
    JWT_SECRET_KEY=mysecret
    ENVIRONMENT=sqlite
    ```
    
### 1.2. База данных и запуск

1.  **Создайте файлы миграции и примените их:**
    
    Сначала создайте файл, который опишет новые таблицы (`reports`):
    ```bash
    alembic revision --autogenerate -m "Create reports table"
    ```
    
    Затем примените изменения к базе данных:
    ```bash
    alembic upgrade head
    ```

2.  **Запустите сервер:**
    ```bash
    fastapi dev main.py
    ```

    Сервер доступен по адресу: **http://127.0.0.1:8000**
    **Документация API (Swagger UI):** **http://127.0.0.1:8000/docs**

---